import { motion } from 'framer-motion';
import styled from 'styled-components';

const ServicesStyle = styled(motion.section)``;

const Services = () => {
    return <ServicesStyle key="services"></ServicesStyle>;
};

export default Services;
